# 柚子樂器全通路商品上架規則

本文件是營運中心、Codex 與店內固定 IP 上架代理共同遵守的單一規則來源。目前唯一可執行的固定流程版本為 `youzi-four-channel-listing-v2`。v1 與更早的工作必須回到營運中心建立 v2 新快照，不得沿用、混合或降級。商品不同只會更換案件資料，不得改變執行次序或臨時另走第二套流程；只有使用者明確修改規則時，才建立新的流程版本。商品以中央商品主檔的 SKU／商品編號為唯一識別。中央主檔已有平台商品 ID 時，直接更新該指定商品；沒有平台商品 ID 時，依本次唯一的新 SKU 直接建立，不先掃描平台目錄。只有送出結果不明或逾時復原時，才用完全相同 SKU 精確查找；若此時找到多筆或身分仍無法確認，才停止並標示需要處理。

## 商品圖片固定格式

1. 每個商品案件最多保留 20 張來源圖；所有合併商品的繁體完成圖先合成一個共用池，四通路整組最多使用 12 個不同完成圖 URL，不是每個編號各 12 張。建立共用池時先保留至少一張 `cleanMain` 與一張 `brandedHero`，再以每個細項先一張的輪流方式公平填滿，最後才加入同細項的其他圖片；細項超過 12 個時也不得讓單一細項先占滿容量。固定店面宣傳圖與商品介紹末端的兩張服務圖另計。
2. 完成圖必須標示一個或多個固定角色：`cleanMain`、`brandedHero`、`localizedDetail`、`specification`、`variantRepresentative`。同一 `sourceImageUrl` 可以產生多個角色輸出；每一列 `generatedListingImages` 都要保存 `sourceImageUrl`、`roles` 與 `assetFlags`（至少包含 `containsLogo`、`containsText`、`containsContactInfo`、`containsQrCode`、`greenBrandTemplate`、`momoPromotionEligible`），不得把同一來源壓成單一完成圖。
3. `brandedHero` 固定使用方形 1:1 的「柚子樂器淺色商業展示版」：以 `product-listing-main-template.jpg` 為品牌骨架，保留頂端薄荷綠紋理、紅色標語及右上柚子樂器標誌；移除左下寶寶及 `PIC COLLAGE`，不得加入價格、地址、電話、QR Code、浮水印或聯絡資訊。綠色品牌區下方固定設計成淺色商業展示底板，不限定白色；依商品配色選用奶油白、米色、淺灰、淺藍、淺粉或其他低彩度淺色，搭配柔和弧形、展示台、棚拍光影或乾淨景深。
4. `brandedHero` 的商品精準去背後須成為最大主體，放在右側或中央偏右，約占品牌區下方畫面的 55～65%；左側或實際留白處放商品標題與 1～3 個已查證短賣點。最多加入 2 個輔助視覺，只能取自來源圖可辨識的細節特寫、折疊／展開狀態，或不暗示額外配件的淡化樂器使用情境；資料不足就省略，不得虛構功能、零件、贈品或使用狀態。不同商品可調整淺色底板、點綴色、圓角資訊卡及局部排列，但固定維持相同品牌頁首與「左側資訊、右側／中央商品主視覺」骨架，畫面不得雜亂。
5. 所有來源逐張完整檢查。簡體中文須改為台灣繁體中文；中國大陸用語改為自然台灣用語；型號、數字、尺寸、單位、顏色及商品外觀不可改變。無法辨識的小字不得猜測。
6. 不得虛構配件、贈品、認證、保固或規格。圖片若寫「含琴盒」但實際沒有，必須移除。
7. `cleanMain` 必須是無文字、無 Logo、無品牌框、無地址、電話或 QR Code 的乾淨商品圖。EasyStore 與蝦皮首圖固定為 `brandedHero`，第 2 張起才使用 `cleanMain` 與其他繁體完成圖。酷澎與 MOMO 首圖固定為 `cleanMain`；`brandedHero` 在兩者都只能放第 2 張以後且不得含地址、電話或 QR Code。MOMO 第 2 或第 3 張必須先保留一張符合專推圖條件的乾淨完成圖，且整個 MOMO 圖庫最多放一張安全的 `brandedHero` 次圖。若平台明確回傳整個圖庫禁止 Logo／文字，才移除該平台的品牌次圖。缺少指定首圖角色就停止該平台，不得用另一角色、店面宣傳圖或舊圖補位。
8. 超出上方輪播容量的商品照片全部放入下方商品介紹。商品介紹採圖文穿插，最末端固定依序加入 `product-listing-description-promo-1.jpg` 與 `product-listing-description-promo-2.jpg`。
9. 圖片不足時可以少放；圖片足夠時避免重複。「準備上架」可啟動框選收圖模式，到淘寶、天貓、1688 或阿里巴巴用滑鼠框出需要的畫面，圖片依 SKU／EasyStore 商品 ID 寫回同一件商品；截錯可刪除，也可直接從電腦上傳。收圖步驟不做簡繁轉換，後續圖片流程再統一處理。
10. 「已有圖片」不等於「已完成圖片處理」。一鍵四通路上架只能使用本次勾選來源產生、已完成台灣繁體化且角色可核對的成品；來源圖片順序、張數或製圖要求有任何改變時必須重新製作。
11. 圖片轉換必須整批成功才可發布。少一張、部分失敗、來源對不上、角色缺漏或缺少完成紀錄時，一鍵流程必須停止並保留待重試狀態；不得以簡體原圖、供應商原圖或舊成品補位後繼續發布。
12. 每張來源圖片只進行一次完整檢查。在同一輪圖片編輯中，先逐字掃描所有可見中文，再一次完成簡轉繁、大陸用語改為台灣用語、錯字／亂碼／殘缺文字修正，以及該張圖片需要的各角色輸出；不得另外產生第二次圖片檢查流程。只有記錄為 `single-pass` 完成的成品可進入四通路發布清單。
13. 中央商品 `imageUrl` 與細項代表圖只使用對應來源的 `cleanMain`；EasyStore／蝦皮使用的 `brandedHero` 平台首圖只能另存為平台圖庫資料，不得覆寫中央辨識主圖。先寫入新完成圖引用、重新讀取資料庫，再確認中央、所有細項與四平台引用都已全面換新；中央或任一細項的任何圖片欄位只要仍等於任一凍結來源 URL，就不得把 cleanup 推進為 `required`。四平台回條都要同時提供 `appliedImageUrls`（實際套用且只能來自該平台最終完成圖計畫）及 `officialImageUrls`（正式頁完整圖片 URL，可為平台 CDN，但不得為來源圖），並設定 `imageEvidenceComplete=true`；缺少任一組證據時不得標記該站 verified 或整筆 completed。全部核對成功後，該上架 job 才把 cleanup 狀態推進為 `required`，由受控清理 worker 移除來源 binary 與可見簡體原圖。永久只保留來源 URL／hash、順序、角色與輸出譜系 metadata。前端交接本身不可在引用核對前提前刪檔。
14. 清理 worker 只接受後端 service account／system 寫入的 `required` 事件，並只可操作 Firebase 預設 bucket 中該案件自己的 `ops-product-listing-cases/{productId}/references/` 前綴。刪除前須保存 object path、generation、hash、大小與 content type 等 lineage metadata，並使用 generation precondition。外部 URL、其他案件、`completed/`、`generated/` 或任何無法安全判定的路徑都不得刪除；安全條件不符時標記 `blocked`。Storage 暫時錯誤時維持 `cleanupStatus=required`、記錄 `cleanupRuntimeStatus=failed-retryable`，由同一事件自動重試；只有重新列出來源前綴確認為空後才可標記 `completed`。

## 共通商品資料

- 定價與網路售價原則上一致；缺少平台售價時可以從共同網路售價初始化。
- 保固預設為半年；平台欄位若要求英文期間，使用 `6 Months`。仍須依實際店家承擔方式填寫保固類型。
- 品牌與型號可查證時必須填寫。
- NCC、BSMI 等認證只能填入可查證的正式號碼，絕不猜測或填假資料。沒有資料時留白並交由人工確認。
- 平台正式分類與店內分類分開保存；選擇最貼近商品的分類及搜尋關鍵字。

## 平台狀態

商品主檔的 `platformListingStatus` 分別記錄 `easyStore`、`shopee`、`momo`、`coupang`。狀態包含：未查驗、確定未上架、已上架、已有平台商品／編號、已排隊、審核中、草稿、已下架、受限制、未通過及需要處理。

「未查驗」不等於「未上架」。營運中心商品卡直接顯示四平台狀態，並可篩選「缺少／待查驗」或單一平台需處理的商品。v2 新上架不為了判斷這個狀態先掃描平台目錄；中央已有平台 ID 時視為既有商品並更新，沒有 ID 時依新 SKU 建立。只有送出結果不明的復原流程，才以完全相同 SKU 做一次精確核對。

## 自動執行、重試與瀏覽器頁籤

- 本文件與工作佇列內的 `automationPolicy` 是同一套持續累積的規則。測試中確認的新規則要合併回既有流程，不建立另一套互相競爭的臨時規則。
- 每次按「帶入這個 Codex 對話」都必須先等待尚在上傳的收圖完成，再保存表單、重新讀取資料庫中的主案件與所有細項案件，建立不可變的 `handoff input snapshot`。這份輸入快照固定來源圖、使用者選擇、修改要求與當下商品資料；交接指令只能從這份快照產生，不得直接從可能過期的畫面欄位產生。
- 每一個合併案件都要明列來源圖、待繁體化圖片、繁體完成圖及圖片狀態。任何一個案件同時沒有來源圖與完成圖時，必須留在營運中心並指出缺圖商品；不可建立 0 圖的 Codex 工作，也不可拿同系列商品圖片替代。
- Codex 完成圖片後，完成輸出寫回各自案件，但不得改寫 `handoff input snapshot`。第一次進入平台前，後端必須重讀每個案件的最新完成圖，逐張確認 `sourceImageUrl` 確實屬於凍結輸入來源，再建立最終不可變的 job `preparedSnapshot`；來源不在輸入快照、來源與輸出 URL 相同、缺少角色或旗標時一律停止，尚未操作任何平台。
- 開啟任何平台前，先在後端一次完成分類、品牌、屬性、物流、價格、庫存、圖片角色與細項資料的預檢，不可進入平台後才反覆返回營運中心補同一批資料。固定順序為 EasyStore 官網、蝦皮、酷澎、MOMO；EasyStore 是蝦皮的資料前置步驟，其餘平台使用同一份已完成資料。
- 正式發布工作使用逐站狀態機：`easyStore → shopee → coupang → momo → completed`。後端一次只准處理 `currentStage`；前一站尚未從正式清單核對相同 SKU、價格、庫存與狀態時，下一站固定為 `blocked-by-previous-stage`，不得預先建立酷澎或 MOMO 工作。
- 第一次操作 EasyStore 前，正式發布工作就把最終 `preparedSnapshot` 保存在工作紀錄；EasyStore、蝦皮、酷澎及 MOMO 的後續推進與重試只能沿用該快照，不得重新讀取案件、重新研究商品或重建另一份資料。通路核對由 `verifyProductListingStage` 驗證，只有平台清單與正式商品資料兩處都相符才推進下一站。
- 只有 `job schemaVersion`、目前 `automationPolicy`、固定平台順序、最終完成圖快照、穩定資料指紋及案件 `handoff input snapshot` 全部一致的未完成 v2 工作可以復用。指紋採遞迴排序物件 key 的穩定格式（陣列仍保序），避免 Firestore map key 順序造成誤判；任何舊 policy、舊 schema、非最終圖片快照或凍結案件內容不符的早期 v2 工作一律拒絕，不可混入新流程。
- 酷澎與 MOMO 佇列工作必須綁定本次 `jobId`、`stageToken` 與資料指紋；同一份資料重試時復用原工作並清除舊回條，不可讓上一輪完成紀錄誤推進新工作。只有 MOMO 最後核對完成後，整筆工作才可寫入 `finishedAt` 與 `published`。
- 蝦皮固定只使用 EasyStore 官方蝦皮通路同步／編輯頁：分類、品牌、屬性、物流、圖片、價格、庫存、細項與發布都在同一頁完成，不再另外進入蝦皮賣家中心。頁面或平台暫時錯誤時，仍以同一個 EasyStore 通路商品、同一頁與同一份已準備資料續做；不得切換第二條路徑、返回前一平台重新研究、重新修圖或重新整理文案。發布後只到 EasyStore 蝦皮通路商品清單核對一次正式結果。
- 平台內不做來回重複核對：送出前使用預檢資料，送出後只做一次正式清單的最終 SKU／價格／庫存／狀態核對；只有最終結果不一致才從同一草稿的失敗步驟續做。
- 禁止在送出前全面搜尋或瀏覽任一平台商品清單。只有提交結果不明、回應逾時或精確狀態無法判定時，才可用本案件完全相同 SKU 做一次精確查詢；不得用名稱廣搜。
- 助手為工作開啟的頁籤，在該步驟確認完成後必須關閉；每個仍需登入的通路只保留一個登入錨點頁籤，並盡量回到平台首頁或商品列表。不得關閉使用者原本開啟且與任務無關的頁籤。
- 發布失敗或逾時時，先使用本次 job 保存的既有平台商品 ID、草稿 ID、暫存編號與 stage token 復原；只有送出結果仍不明時，才以完全相同 SKU 做一次精確核對。找到原商品或原草稿就從失敗步驟續做，禁止因重試而建立第二筆商品；精確核對找到多筆時停止並標示需要處理。
- 圖片上傳或平台轉檔是非同步步驟時，必須等待處理完成再進入下一步；短暫逾時、頁面重新整理或欄位未保存可自動退避重試，並保留最後可恢復的步驟。
- 平台顯示「發布成功」視窗不等於真正完成。發布後必須回商品列表或可查驗的正式介面，至少核對商品編號、價格、庫存與狀態；介面與列表結果矛盾時記為待重試，不得回報已完成。
- 自動重試只在身分與目標可確定時執行；OTP、CAPTCHA、登入失效或平台持續故障才交由使用者處理。

## MOMO

- 平台商品上限為 1,000；可使用到 1,000 筆，不固定預留 10 個新品空間。
- 庫存數量為 0 只代表售罄，不是自動下架條件。曾經賣過、尤其銷量較高的商品，即使目前庫存為 0，也應維持上架；查不到銷售紀錄時預設保留。
- 只有名額確實不足時，才依 MOMO 銷售紀錄從「零庫存且沒有銷售／銷量很低」的商品開始評估下架。未取得銷售紀錄前不得批次下架；目前名額足夠時不需執行零庫存下架。
- 有庫存但已下架或違規的商品，須先查明平台狀態與必填資料；資料可查證且名額允許時才重新發布，不得用猜測的 NCC／BSMI 等資料強行上架。
- 新商品配送固定選「甲指（第三方）：000001 台中市圓環東路347號」，此選項對應新竹物流。不要改選其他收貨不便的承運方式。
- 大型商品不可使用超商；應使用上述第三方／新竹物流。小型商品才可依平台實際限制使用超商。
- MOMO 登入通常需要手機 OTP。OTP 只由帳號持有人完成；系統不得繞過二階段驗證。
- MOMO 專推圖只可從已完成台灣繁體化的 MOMO 第 2 張或第 3 張中，挑選角色包含 `cleanMain`、`localizedDetail` 或 `specification`，且 `momoPromotionEligible=true` 的乾淨商品完成輸出；`containsLogo`、`containsText`、`containsContactInfo`、`containsQrCode`、`greenBrandTemplate` 必須全為 `false`。若第 2、3 張都沒有合格圖片，停止 MOMO 發布，不得以宣傳圖補位。
- 專推圖要先上傳至 MOMO 素材銀行，再從圖文編輯器使用「從素材銀行選擇」插入。直接按「此裝置上傳」後在畫面中看見圖片，不代表資料已保存；儲存後必須重新開啟同一份草稿，確認圖文介紹仍包含同一張專推圖，才可按發布。
- 平台仍會審核商品；送出後狀態先記為已排隊或審核中，不可直接當成已上架。
- MOMO 曾發生「畫面顯示發佈成功，但草稿中的必填資料沒有保存」：商品列表仍為暫存、庫存變成 0、售價空白，官方商品資料也查不到。每次發佈後必須用提交時的 SKU、售價與庫存逐項比對商品列表，並再以官方商品資料查驗；只有兩處都一致才可記為完成。
- 若出現上述特徵，必須回到完全相同的 MOMO 暫存品號，重新套用屬性、其他資訊、庫存、售價、市價、原廠編號、材積、重量、溫層、超商／甲指第三方、`000001` 出貨地、圖文介紹、特色與半年保固後再發佈。不得另建新品或複製草稿；預期庫存本來就是 0 時，應比對為 0，不可單憑零庫存誤判失敗。

## Coupang／酷澎

- 中央主檔已有酷澎商品 ID 時直接更新指定商品；沒有 ID 時直接以本次唯一的新 SKU 建立，不先搜尋酷澎全目錄。只有送出結果不明的復原流程才精確查同 SKU；多筆或不確定時停止。
- 酷澎第 1 張必須是 `cleanMain`，不得有 Logo、文字、地址、電話或 QR Code；`brandedHero` 可放第 2 張，只有平台明確回傳整個圖庫禁用 Logo／文字時才移除。缺少乾淨商品主圖時停止酷澎，不以店面宣傳圖代替。
- 售價必須是 10 元倍數。
- 大型商品不可使用超商，依實際尺寸改用宅配／大型物流。
- 平台審核中的商品記為已排隊或審核中，審核完成後再更新為已上架或未通過。

## 官網與蝦皮

- EasyStore 中央主檔已有 `productId`／`variantId` 時直接更新指定商品；沒有平台 ID 時直接以本次唯一的新 SKU 建立，不先搜尋全部商品。只有 POST 結果不明或逾時復原時才精確查同 SKU；若找到多筆則停止。
- 蝦皮固定只使用 EasyStore 官方蝦皮通路同步／編輯頁；分類、品牌、屬性、物流、圖片、價格、庫存、細項與發布在同一頁一次完成，不再開啟蝦皮賣家中心或第二條助手路徑。
- 蝦皮中央主檔已有平台商品 ID 時在同一個 EasyStore 官方蝦皮通路頁更新；沒有平台 ID 時在同頁直接建立本次唯一的新 SKU，不先開啟商品清單搜尋，也不要求人工選擇「新增或更新」。
- 小型商品符合超商材積與重量限制時，可同時開啟超商與新竹物流；大型商品關閉超商，只使用符合材積級距的新竹物流。
- 黑貓宅急便、嘉里物流及「賣家宅配：大型／超重物品運送」目前都未配合，一律不得自動開啟。尺寸、重量或新竹物流級距無法確認時停止物流自動填寫，不得改選其他承運方式。
