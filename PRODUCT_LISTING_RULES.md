# 柚子樂器全通路商品上架規則

本文件是營運中心、Codex 與店內固定 IP 上架代理共同遵守的單一規則來源。目前唯一可執行的固定流程版本為 `youzi-four-channel-listing-v3`。v2 與更早的工作必須回到營運中心建立 v3 新快照，不得沿用、混合或降級。商品不同只會更換案件資料，不得改變執行次序或臨時另走第二套流程；只有使用者明確修改規則時，才建立新的流程版本。商品以中央商品主檔的 SKU／商品編號為唯一識別，並以案件明確保存的 `listingIntent` 決定這次是新增、成組、加入細項或原地修改；不得再根據「目前有沒有平台商品 ID」自行改變使用者選定的處理方式。只有送出結果不明或逾時復原時，才用完全相同 SKU 精確查找；若此時找到多筆或身分仍無法確認，才停止並標示需要處理。

## 四種商品處理方式

1. `create-single`（新增獨立商品）：以本案唯一 SKU 建立一個完整的新商品。平台若已保存任何既有商品 ID，必須在送出前停止，不能把「新增」改成更新舊商品，也不能再建立第二筆重複商品。
2. `create-group`（新增多細項商品）：建立一筆主商品與本次快照列出的完整、封閉 SKU 集合。共同名稱、介紹、分類、物流與圖文只建立一次；每個細項分別保存細項名稱、SKU、價格、庫存、條碼與代表圖。平台若已有主商品 ID，必須停止並改由管理者重新選擇正確處理方式，不能擅自併入舊商品。
3. `add-variant`（加入既有商品細項）：只能使用管理者指定父商品的既有平台 ID。原商品、原細項、原圖片與未要求變更的介紹全部保留；把新細項的名稱、SKU、價格、庫存、圖片、差異與新介紹合併追加到原商品的圖文介紹。不得覆蓋原內容、不得改用相似父商品、不得另外建立一筆父商品。EasyStore 新細項建立或更新後，必須同步更新同一父商品的 `body_html`，確認新細項圖文已進入原詳情。
4. `update-existing`（修改既有商品）：以同一 SKU 及各平台已保存的精確商品 ID 原地修改。依 `listingChangeInstructions` 與新圖片，只替換錯誤、過時或改版的文字與圖片，移除已被取代的舊版本，保留沒有要求修改的內容。任何通路缺少唯一既有商品 ID 時都必須停止，不能建立新品、追加重複版本或改到名稱相似的商品。
已完成上架的案件再次從單一商品卡開啟時，必須建立新的單一 SKU 修改畫面：自動切成 `update-existing`，只載入目前 SKU 自己的來源圖、完成圖與平台資料，並清除上一次暫時使用的 `variantGroupItems`、父子細項與合併選擇。歷史上架快照與平台 ID 仍保留在資料庫供稽核，但不得再次出現在本次編輯畫面；只有管理者重新從「合併所選商品」進入，才可建立新的多細項工作。

## 固定圖文編排（四種處理方式共用）

- `create-single`、`create-group`、`merge-existing`、`add-variant`、`update-existing` 全部套用 `youzi-interleaved-description-v2`；新商品與既有商品不得改走舊版純文字介紹。
- 橫向商品圖片最多先放 6 張商品圖，最後一張固定為 `product-listing-store-promo.png` 店址圖。店址圖不得成為商品首圖、MOMO 廣告圖或 MOMO 專推圖。
- 詳細介紹固定依序排列：商品介紹與商品特色 → 第 1 張商品圖 → 商品規格 → 第 2 張商品圖 → 使用方式／適用情境與使用建議 → 第 3 張商品圖 → 其餘商品圖 → 顧客版實體圖（若有）→ `商品圖片與規格僅供參考，實際內容以收到的實體商品為準。` → `product-listing-description-promo-1.jpg` → `product-listing-description-promo-2.jpg`。
- 兩張固定介紹圖是詳細介紹的絕對最後兩個內容區塊，不只是最後兩張圖片；第二張固定介紹圖之後不得再放文字、圖片、空白宣傳區塊或免責句。實體商品免責句固定放在第一張固定介紹圖之前，且是固定介紹圖之前的最後一個文字區塊。
- 詳細介紹至少要有 3 個真正商品圖片區塊，才能完成三段圖文穿插。商品圖不足時，先從本案已保存來源、官網既有圖片或精確商品來源補齊並完成繁體化；仍不足時可依上方橫向商品圖順序重複使用已完成商品圖，重複圖片可以出現於不同段落。不得用店址圖或兩張固定介紹圖補數量。
- EasyStore 官網必須先保存完整安全 HTML、全部文字及固定圖片順序。蝦皮使用同一份官網內容，並在送出前實際確認文字與每一張預期圖片都已保存；只匯入文字、顯示 0 張圖片或只看到成功按鈕回應都不算完成。
- 酷澎可編輯詳細說明時必須寫入同一圖文順序，保存後重開一次確認；MOMO 必須使用原生圖文編輯器組成同一順序，保存後重開一次確認圖片仍存在。MOMO 專推圖仍是獨立欄位，必須使用合規乾淨商品圖，不得以店址圖或固定介紹圖代替。
- 任一目標平台缺少商品介紹文字、三個穿插商品圖片區塊、橫向圖庫最後的店址圖、詳細介紹絕對最後兩張固定介紹圖，或固定介紹圖前的免責句時，都不得標記 `verified`。全部選定平台均逐一回讀驗證後，整份工作才可標記完成。
- 「修改圖片及網路介紹」是既有商品的快速入口，固定使用 `update-existing` 與四平台範圍，只能更新同一 SKU 及既有平台商品 ID。它提供「加入待處理」與「立即處理」，不會建立新商品；其餘四種商品處理方式仍自動套用相同圖文規則。

`listingIntent` 在建立 handoff input snapshot 時即凍結，四個平台與所有重試都必須沿用同一值。平台頁、店內代理及重試流程都沒有權限把它改成另一種模式。新增模式遇到既有 ID、修改模式缺少 ID、父商品 ID 重複或精確 SKU 對不上時，一律在首次送出前停止。管理者按下「帶入這個 Codex 對話」已完成例行發布授權，不因上述模式再增加第二次確認。

## 固定商品內容標準

- 全部商品使用 `youzi-rich-product-content-v3`。新商品必須在第一次上架前完成；舊商品依 `update-existing` 原地升級，保留原 SKU、細項與各平台商品編號，不因改寫內容另建新品。
- AI 必須先逐張分析本案原始圖片、圖片文字辨識結果與原有商品文字，再以精確型號或條碼確認商品身分；製造商官方頁、規格表或說明書為主要補強來源，台灣代理商／經銷商與多個可信使用指南、使用經驗可作補充。不得跳過本案圖片與原文直接生成，也不得把單一匿名評論當作規格事實。
- 固定完成 10 個不重複、可驗證的商品介紹／特色，以及 10 個有來源的使用方法、適用情境或使用心得；每點以約 20 個中文字為原則，再獨立列出全部可驗證規格。每個事實都要有 `fieldEvidence`；事實不足時必須繼續查證或標示缺資料並停止發布，絕不猜測、湊數或虛構配件、功能、認證與使用心得。
- 一份已驗證母內容固定包含「商品特色」「使用方式／適用情境」「商品規格」，再轉成 EasyStore 安全 HTML、蝦皮進階圖文、MOMO 原生圖文與酷澎安全 HTML。不得進平台後各寫一套。
- 標題、內文與圖卡不加入「柚子樂器」或其他店名、店鋪 Logo、聯絡方式、QR Code 與導外訊息。官方可驗證的製造商 Logo 只在平台規則允許時使用。
- 所有一般文字、商品圖片與圖文區塊完成後，必須先放唯一的「商品圖片與規格僅供參考，實際內容以收到的實體商品為準。」免責句，接著才放兩張固定介紹圖。儲存與轉換時先刪除舊位置的重複免責句與固定介紹圖，再依此順序加回；第二張固定介紹圖必須是詳細介紹的絕對最後內容。

同款多細項案件中，交接快照列出的 SKU 是封閉且完整的商品身分集合。整組只能建立一筆根 job、一筆平台主商品與一個細項集合，前端不得再對每個 SKU 分別呼叫發布服務。商品名稱、介紹、分類、品牌、物流、保固與共用圖庫只準備及填寫一次；每個細項只分別保存細項名稱、指定完成圖、SKU、售價、庫存及條碼。平台送出後要核對同一個平台商品內的 SKU 集合完全等於快照集合，並逐一核對各細項價格、庫存與獨立代表小圖；任一細項沒有小圖、仍使用凍結來源原圖、缺少、重複或混入任何本案以外 SKU 都不得 verified。這套結構必須同時支援 2、3、5 個以上細項，不得為每個數量另寫一條流程。只能把這些 SKU 建立在同一商品內，不得使用未列入案件的基礎 SKU、相似名稱、相同品牌或歷史商品替代，也不得把新細項加入無關的舊商品。每個細項卡最前面直接顯示自己的獨立代表圖與細項名稱；可在該卡開始收圖，或把任一細項圖片拖到另一張卡複製使用，放下後立即加入並在尚未指定時成為代表圖。這是「加入細項小圖」，不得取代或移除整組共用商品圖。使用者點選的細項代表圖只保存為來源圖選擇；圖片完成後必須以完全相同的 `sourceImageUrl` 對應到乾淨繁體完成圖，再將該完成圖設為該細項主圖，找不到對應完成圖時不得直接使用來源原圖上架。單一商品、同款多細項、併入既有商品及新增既有商品細項，都必須沿用同一套小圖對應與核對規則；不得只在新建同款商品時處理而漏掉舊商品合併。每張卡的收圖按鈕、目前張數、進行／完成／錯誤狀態都必須留在該卡內，不得顯示到整組下方。區分欄位以商品名稱自動辨識為「顏色／尺寸／調性」，無法判斷才顯示「規格」，不得再使用不清楚的預設詞「款式」。介面只保留細項名稱、收圖、圖片與代表圖；整組圖片在下方以單一「上架圖片預覽」呈現，不再重複第二套收圖入口或分組說明。上架圖片預覽第一次開啟時，容量上限內的彙整圖片全部預選；只有使用者按叉叉的圖片才排除，且重新整理後不得自動勾回。

EasyStore 同步必須以完全相同 SKU 找到官網細項，並用該細項的 `image_id` 精確對應同一父商品的圖片。已有官網細項圖時，合併／加入細項畫面的代表小框直接自動帶入，不再要求使用者重選；只有單一細項商品可以用官網主圖補入。多細項商品若沒有綁定自己的細項圖，該小框必須維持缺圖，不能借用父商品主圖或其他顏色／規格的圖片，並留在待處理範圍供後續補圖。

## 程式化上架與平台頁面合約

1. 商品按下「帶入這個 Codex 對話」後，先在平台外只做一次商品判斷：完成圖片、共同標題與介紹、品牌／型號證據、樂器類標準分類、各平台分類對照、分類必填屬性、價格、庫存、細項名稱與代表圖、材積物流及四平台圖片排序。結果寫入同一份不可變的 `preparedPlatformFieldPlan`，進入任何平台後不得重新研究或重新判斷固定欄位。
2. 平台頁面的區段、欄位順序、穩定標籤、圖片限制與固定選項只盤點一次並保存為 `platformPageContracts`。每次執行只先做輕量登入／版型簽章檢查；簽章相同就依欄位表順序直接填寫，不重新掃描整頁。只有版型簽章改變、分類衍生欄位出現或平台明確回傳錯誤時，才重新辨識目前區段，不能回頭重做商品分析或已完成平台。
3. 固定欄位由程式直接套用：半年保固、立即發布、MOMO 甲指第三方 `000001`、平台首圖角色、既定平台工作區、禁用未配合物流與最後正式送出。需要判斷的欄位只限商品事實、標準分類、分類衍生必填屬性、依已知材積計算的物流及平台明確錯誤；不得把固定選項再次交給 Codex 臨場思考。
4. 登入是獨立前置狀態，不是商品步驟。開始填表前先檢查登入錨點；可用內建瀏覽器保存資料恢復時自動恢復並回到同一商品／草稿。只有 OTP、CAPTCHA 或保存資料無法恢復才停止。登入恢復後沿用相同 job、快照與目前平台，不重建商品。
5. 實際盤點的 MOMO 合約依序為：基本資料（品號、1:1 主圖、專推圖、品牌、60 字商品名、分類與認證）→分類屬性與其他資訊→銷售規格、細項圖、庫存、售價、市價、原廠編號與條碼→材積、重量、溫層、超商／甲指第三方與免運→圖文介紹、MOMO 推薦特色、短標語、店內分類、半年保固→立即發布。MOMO 主圖為 1～6 張，每張 38KB～1MB；專推圖是獨立欄位，必須在第一次送出前完成素材銀行插入及重開草稿核對。
6. 實際盤點的 EasyStore 合約依序為：商品名稱→HTML 圖文介紹→商品圖庫→選項與細項→各細項庫存、SKU、售價、原價、成本與條碼→尺寸、重量→稅務、免運與庫存追蹤→SEO 網址與摘要→發布狀態、銷售通路、分類、品牌、供應商、標籤及備註→儲存。蝦皮固定使用 EasyStore 的 Shopee Taiwan 已連接通路：沿用同一 EasyStore 商品；官網完整介紹、介紹圖片、固定最後免責句及順序必須在進站前寫入不可變快照。進入蝦皮頁後只做一次輕量資格檢查；帳號有「進階商品描述」時立即開啟並按「使用 EasyStore 的產品描述」，把官網文字與介紹圖片一起帶入，再依已準備欄位表填蝦皮分類、屬性、價格調整、細項圖、重量與物流，依序準備發布、上架，最後回通路商品清單核對一次。蝦皮頁內不得重新分析、改寫介紹、重排圖片或詢問例行確認；只有帳號沒有該功能、欄位版型改變或平台明確回傳圖片失敗時才停止處理該例外。
7. 酷澎目前已由正式建立頁驗證，固定走「以圖片建立」：先放 `cleanMain` 主圖與繁體完成副圖→選樂器／樂器配件葉分類→填可驗證品牌（確實無品牌或平台拒絕已記錄時才用無品牌）→按一次「產生商品資訊」→一次填完細項、售價、庫存及賣家 SKU→配送→人工整理的商品介紹與 `TW_General` 合規資料→「建立產品」→以完全相同 SKU 在正式商品清單核對一次。開始前以 Coupang Wing／商品管理作登入探針；登入頁僅代表工作階段失效，恢復後回到同一草稿，不得因此切換路徑、名稱廣搜或建立替代商品。若產生後的細項欄位呈唯讀或值無效，只可回同一草稿的圖片步驟重新產生一次；不得整頁重跑或另建商品。
8. 程式可確定的欄位與平台互動使用自動化；圖片內容理解、可驗證商品特色及無法由規則唯一決定的分類屬性才由 Codex 判斷。兩者的輸出都必須先寫入同一欄位表，平台操作階段只負責套用和回讀平台錯誤，不能每一站重新自由解讀交接文字。

## 商品圖片固定格式

### 實體圖

- 商品案件的實體圖入口與一般圖片放在同一區，只顯示一個小型「＋ 實體圖」方塊及已加入的縮圖；不得再建立大型、獨立且重複的「實體圖片」區塊。手機專用入口仍保留，搜尋或點最近商品後可立即拍照。
- 每張照片必須保存兩份可追溯資料：`physicalOriginalImageUrls` 保存完全未修改的原照；`physicalImageUrls` 是顧客版，只允許以確定性程式加上清楚可讀的「實體圖」三字。字體不得縮成角落小字。實體圖不得進入圖片模型，不得去背、美化、換背景、改色、修字、重畫商品或加入其他文案。
- 顧客版實體圖不計入來源圖、繁體完成圖角色或平台輪播圖，永遠不能當 EasyStore、蝦皮、MOMO 或酷澎主圖。它只加入四平台商品詳情／圖文介紹，固定排在一般繁體完成介紹圖之後、最終實體商品免責句之前。
- 案件有幾張顧客版實體圖，就在平台圖文介紹的圖片上限內依保存順序使用幾張；超過平台容量的部分保留在商品後台，不得為了塞入而改圖、取代主圖或移除原照。刪除畫面上的實體圖只移出本商品目前使用清單，原始照片仍保留供追溯。
- `add-variant` 時，新細項的實體圖與其他新圖一樣合併進原父商品詳情；`update-existing` 時，只依修改要求替換相關實體圖。實體圖不得造成第二筆商品或重複細項。

1. 每個商品案件最多保留 20 張來源圖；所有合併商品的繁體完成圖先合成一個共用池，四通路整組最多使用 12 個不同完成圖 URL，不是每個編號各 12 張。建立共用池時先保留至少一張 `cleanMain`、一張 `storefrontPortrait` 與一張 `brandedHero`，再以每個細項先一張的輪流方式公平填滿，最後才加入同細項的其他圖片；細項超過 12 個時也不得讓單一細項先占滿容量。不再自動加入店面宣傳圖或服務圖。
2. 完成圖必須標示一個或多個固定角色：`cleanMain`、`storefrontPortrait`、`brandedHero`、`localizedDetail`、`specification`、`variantRepresentative`。同一 `sourceImageUrl` 可以產生多個角色輸出；每一列 `generatedListingImages` 都要保存 `sourceImageUrl`、`roles` 與 `assetFlags`（至少包含 `containsLogo`、`containsText`、`containsContactInfo`、`containsQrCode`、`greenBrandTemplate`、`momoPromotionEligible`），不得把同一來源壓成單一完成圖。
3. `storefrontPortrait` 固定使用 `product-listing-brand-template-portrait.png`（750×1000、3:4）；`brandedHero` 固定使用 `product-listing-brand-template-square.png`（1000×1000、1:1）。兩張母版版本固定為 `youzi-locked-green-brand-template-v1`，完成圖必須保存母版版本、SHA-256 與合成方式；缺少或不符時不得進入平台。
4. 品牌母版的綠色頁首背景、頁首高度、紅色標語「有音樂的生活更有風格」、右上圓形柚子樂器 Logo、下方淺色內容面板與細綠邊均為不可變區域；不得換色、縮窄、移位、換 Logo、改字或讓 AI 重畫。只准在內容面板內更換商品、已查證文字及最多 2 個有來源依據的輔助視覺。不得加入地址、電話、QR Code、價格、導外訊息、左下娃娃或 PIC COLLAGE。
5. 圖片完成並寫回案件後，Codex 必須把本案所有完成圖依上架順序排成一張聯絡表／總覽圖，並在目前對話一次顯示；不得逐張分散發送，也不得只顯示 EasyStore 主圖。總覽圖只是進度回報，不是人工審核關卡；顯示後直接繼續建立最終快照與發布，不得要求使用者再次確認或等待回覆。
6. 所有來源逐張完整檢查。簡體中文須改為台灣繁體中文；中國大陸用語改為自然台灣用語；型號、數字、尺寸、單位、顏色及商品外觀不可改變。無法辨識的小字不得猜測。
7. 不得虛構配件、贈品、認證、保固或規格。圖片若寫「含琴盒」但實際沒有，必須移除。
8. `cleanMain` 必須是無文字、無 Logo、無品牌框、無地址、電話或 QR Code 的乾淨商品圖。每件商品都要在進站前自動製作各平台需要的完整成品，不得只把來源原圖直接上傳：EasyStore 首圖製作為 750×1000 的 `storefrontPortrait`；蝦皮首圖製作為 1000×1000 的 `brandedHero`；酷澎與 MOMO 首圖製作為乾淨 1000×1000 的 `cleanMain`；另外依事實製作繁體 `localizedDetail`、`specification`、細項代表圖及 MOMO 專推圖。各平台尺寸、檔案大小、文字、圖片角色與排序全部驗證合格後才可上傳。
9. `storefrontPortrait` 不得送往 MOMO 或酷澎；`brandedHero` 在兩者都只能放第 2 張以後且不得含店名、地址、電話或 QR Code。MOMO 第 2 或第 3 張必須先保留一張符合專推圖條件的乾淨完成圖，且整個 MOMO 圖庫最多放一張安全的 `brandedHero` 次圖。若平台明確回傳整個圖庫禁止 Logo／文字，才移除該平台的次圖。缺少指定首圖角色就停止該平台，不得用另一角色、店面宣傳圖或舊圖補位。
10. 超出上方輪播容量的商品照片全部放入下方商品介紹。商品介紹採圖文穿插，不自動加入額外店面宣傳圖或服務圖；一般商品圖、顧客版實體圖及免責句之後，固定以兩張指定介紹圖作為絕對最後內容。
11. 上方橫向圖庫的真正商品圖不足時可以少放，但最後仍固定加店址圖；下方詳細介紹若不足三個商品圖片區塊，可以重複使用上方已完成商品圖補足，重複是允許的。「準備上架」可啟動搜圖模式，在任何一般商品網頁直接點選有綠框的圖片，或按「框選截圖」拉出範圍並確認；圖片依 SKU／EasyStore 商品 ID 寫回同一件商品。截錯可刪除，也可直接從電腦上傳。收圖步驟不做簡繁轉換，後續圖片流程再統一處理。
12. 「已有圖片」不等於「已完成圖片處理」。一鍵四通路上架只能使用本次勾選來源產生、已完成台灣繁體化且角色可核對的成品；來源圖片順序、張數或製圖要求有任何改變時必須重新製作。
13. 圖片轉換必須整批成功才可發布。少一張、部分失敗、來源對不上、角色缺漏或缺少完成紀錄時，一鍵流程必須停止並保留待重試狀態；不得以簡體原圖、供應商原圖或舊成品補位後繼續發布。
14. 每張來源圖片只進行一次完整檢查。在同一輪圖片編輯中，先逐字掃描所有可見中文，再一次完成簡轉繁、大陸用語改為台灣用語、錯字／亂碼／殘缺文字修正，以及該張圖片需要的各角色輸出；不得另外產生第二次圖片檢查流程。只有記錄為 `single-pass` 完成的成品可進入四通路發布清單。
15. 中央商品 `imageUrl` 與細項代表圖只使用對應來源的 `cleanMain`；EasyStore／蝦皮使用的 `brandedHero` 平台首圖只能另存為平台圖庫資料，不得覆寫中央辨識主圖。先寫入新完成圖引用、重新讀取資料庫，再確認中央、所有細項與四平台引用都已全面換新；中央或任一細項的任何圖片欄位只要仍等於任一凍結來源 URL，就不得把 cleanup 推進為 `required`。四平台回條都要同時提供 `appliedImageUrls`（實際套用且只能來自該平台最終完成圖計畫）及 `officialImageUrls`（正式頁完整圖片 URL，可為平台 CDN，但不得為來源圖），並設定 `imageEvidenceComplete=true`；缺少任一組證據時不得標記該站 verified 或整筆 completed。全部核對成功後，該上架 job 才把 cleanup 狀態推進為 `required`，由受控清理 worker 移除來源 binary 與可見簡體原圖。永久只保留來源 URL／hash、順序、角色與輸出譜系 metadata。前端交接本身不可在引用核對前提前刪檔。

16. 蝦皮「使用 EasyStore 的產品描述」按鈕被按下不算完成。助手必須在同一頁核對進階商品描述編輯器同時含文字與 `preparedSnapshot` 中的全部介紹圖片；如果 EasyStore 只帶入文字，直接把缺少的繁體完成圖補進同一個編輯器並重新核對一次。仍缺圖片就停止蝦皮發布並保留同一草稿，不能發布純文字版本，也不能回營運中心重做圖片或文案。單一商品、同款多細項與加入既有細項都套用同一規則。
17. 商品介紹只整理一次，再於進入平台前產生四個固定交付格式：EasyStore 使用含安全標題、段落、清單及繁體完成圖的完整 HTML；蝦皮使用進階圖文描述並核對文字與全部預期圖片，禁止發布純文字版本；MOMO 把相同內容轉成原生文字與圖片區塊；酷澎優先使用安全 HTML，平台不接受原始 HTML 時改送等價且保持原順序的 `IMAGE`／`TEXT` 結構化區塊。MOMO 圖文與專推圖素材固定寬 1000px、高 1500px 以下且小於 500KB。單一商品、同款多細項、合併既有商品及加入既有細項都使用同一份內容計畫，不得進平台後重新寫文案或漏掉圖片。
18. 清理 worker 只接受後端 service account／system 寫入的 `required` 事件，並只可操作 Firebase 預設 bucket 中該案件自己的 `ops-product-listing-cases/{productId}/references/` 前綴。刪除前須保存 object path、generation、hash、大小與 content type 等 lineage metadata，並使用 generation precondition。外部 URL、其他案件、`completed/`、`generated/` 或任何無法安全判定的路徑都不得刪除；安全條件不符時標記 `blocked`。Storage 暫時錯誤時維持 `cleanupStatus=required`、記錄 `cleanupRuntimeStatus=failed-retryable`，由同一事件自動重試；只有重新列出來源前綴確認為空後才可標記 `completed`。

## 共通商品資料

- 定價與網路售價原則上一致；缺少平台售價時可以從共同網路售價初始化。
- 保固預設為半年；平台欄位若要求英文期間，使用 `6 Months`。仍須依實際店家承擔方式填寫保固類型。
- 品牌與型號可查證時必須填寫。
- NCC、BSMI 等認證只能填入可查證的正式號碼，絕不猜測或填假資料。沒有資料時留白並交由人工確認。
- 平台正式分類與店內分類分開保存；選擇最貼近商品的分類及搜尋關鍵字。

## 平台狀態

商品主檔的 `platformListingStatus` 分別記錄 `easyStore`、`shopee`、`momo`、`coupang`。狀態包含：未查驗、確定未上架、已上架、已有平台商品／編號、已排隊、審核中、草稿、已下架、受限制、未通過及需要處理。

「未查驗」不等於「未上架」。營運中心商品卡直接顯示四平台狀態；正式清單存在顯示「有」，確定找不到、違規、拒絕或不可恢復顯示「沒有」，酷澎仍在審核時顯示黃色「審核中」。每件上架工作結束後，必須以完整精確 SKU 在 EasyStore 官網、EasyStore 官方蝦皮通路、MOMO 與酷澎正式清單各核對一次，回寫 `platformListingStatus`、`listingId`、`lastCheckedAt` 與原因；蝦皮不得只因官網存在就推定為「有」。是否新增或更新仍只由已凍結的 `listingIntent` 決定，狀態核對不得反過來改變上架意圖。

## 自動執行、重試與瀏覽器頁籤

- 本文件與工作佇列內的 `automationPolicy` 是同一套持續累積的規則。測試中確認的新規則要合併回既有流程，不建立另一套互相競爭的臨時規則。
- 每次按「帶入這個 Codex 對話」都必須先等待尚在上傳的收圖完成，再保存表單、重新讀取資料庫中的主案件與所有細項案件，建立不可變的 `handoff input snapshot`。這份輸入快照固定來源圖、使用者選擇、修改要求與當下商品資料；交接指令只能從這份快照產生，不得直接從可能過期的畫面欄位產生。
- 每一個合併案件都要明列來源圖、待繁體化圖片、繁體完成圖及圖片狀態。任何一個案件同時沒有來源圖與完成圖時，必須留在營運中心並指出缺圖商品；不可建立 0 圖的 Codex 工作，也不可拿同系列商品圖片替代。
- Codex 完成圖片後，完成輸出寫回各自案件，但不得改寫 `handoff input snapshot`。第一次進入平台前，後端必須重讀每個案件的最新完成圖，逐張確認 `sourceImageUrl` 確實屬於凍結輸入來源，再建立最終不可變的 job `preparedSnapshot`；來源不在輸入快照、來源與輸出 URL 相同、缺少角色或旗標時一律停止，尚未操作任何平台。
- 開啟任何平台前，先在後端一次完成分類、品牌、屬性、物流、價格、庫存、圖片角色、細項資料與四平台固定欄位表，不可進入平台後才反覆返回營運中心補同一批資料。資料完成後同時啟動 MOMO、酷澎與 EasyStore 三個獨立根節點；蝦皮只依賴 EasyStore，EasyStore 核對完成後即可在官方蝦皮通路頁發布，不必等待 MOMO 或酷澎。
- 一批商品第一次開始發布前，必須先完成四通路瀏覽器工作階段預檢，確認 MOMO、酷澎及 EasyStore（含官方蝦皮通路）都已登入且頁面可讀、可點擊、可輸入；尚未通過預檢時不得開始第一件商品。若原頁卡住或登入失效，依序沿用目前已登入頁籤、重開同一官方入口、桌面捷徑／瀏覽器歷史中的既有入口、目前 job 保存的草稿網址、專案紀錄／先前成功上下文中的同平台路徑，以及同一 Codex 內建瀏覽器保存的帳號密碼，自動回到同一商品或草稿；不得重新建立案件或重複商品。只有 MOMO OTP、CAPTCHA、保存帳密被拒絕或平台停用帳號，才可停止並交給使用者。
- 同一批後續每件商品與每個平台階段開始前只做輕量存活檢查，不重新掃描整頁：確認網址／登入頁特徵、主要表單區可讀，以及一次無副作用的欄位聚焦或區段互動可成功。檢查失敗時先重新載入一次；仍失敗便在同一 Codex 內建瀏覽器以相同帳號開啟已知平台入口或完全相同草稿網址，沿用既有 job、SKU、草稿 ID 與 preparedSnapshot 繼續。只有 MOMO OTP、CAPTCHA、平台明確拒絕，或保存帳密確實無法恢復登入時才可停止。
- 不以無限背景重新整理維持登入，也不依賴長時間閒置頁籤一定存活；可靠性來自「發布前先驗證、可恢復登入自動恢復、失效頁籤自動換成相同網址的新工作頁籤」。工作仍在執行時不得因可恢復的頁籤失效詢問使用者是否重開或是否繼續。
- 正式發布工作使用交錯並行狀態圖：根節點為 `momo`、`coupang`、`easyStore`，唯一相依邊為 `easyStore → shopee`。三個根節點可用任意順序完成，單一根平台等待或失敗不得阻擋另兩個根平台；只有蝦皮在 EasyStore 尚未核對完成時標為 `blocked-by-dependency`。四個 stage 全部 verified 後才可寫入 `completed`。
- 瀏覽器採「單一操作鎖＋等待重疊」：導航、點擊、輸入、檔案選擇、選圖及最後送出同一時間只准一個平台持有操作鎖，避免頁籤焦點與檔案選擇器互搶；某平台進入網路回應、圖片上傳、轉檔或平台處理等待時，立即釋放操作鎖並切換另一根平台。這不是三頁同時亂點，而是把不可避免的等待時間重疊。
- 第一次操作任一平台前，正式發布工作就把最終 `preparedSnapshot`、`preparedPlatformFieldPlan`、執行圖與瀏覽器排程規則保存在工作紀錄；MOMO、酷澎、EasyStore 及蝦皮的後續推進與重試只能沿用該快照，不得重新讀取案件、重新研究商品或重建另一份資料。各通路只有在平台清單與正式商品資料兩處都相符時才把自己的 stage 設為 verified。
- 只有 `job schemaVersion`、目前 `automationPolicy`、固定平台順序、最終完成圖快照、穩定資料指紋及案件 `handoff input snapshot` 全部一致的未完成 v3 工作可以復用。指紋採遞迴排序物件 key 的穩定格式（陣列仍保序），避免 Firestore map key 順序造成誤判；任何舊 policy、舊 schema、非最終圖片快照或凍結案件內容不符的 v2 或更早工作一律拒絕，不可混入新流程。
- MOMO 與酷澎佇列工作必須綁定本次 `jobId`、`stageToken` 與資料指紋；同一份資料重試時復用原工作並清除舊回條，不可讓上一輪完成紀錄誤推進新工作。回條允許以任意順序到達，不得再用前後站順序拒絕有效回條；四站全部核對完成後，整筆工作才可寫入 `finishedAt` 與 `published`。
- 四平台圖片在繁體化與版面調整的同一輪就統一為共用交付規格：1000×1000 px、1:1、sRGB、優先 JPEG、每張不超過 1,000,000 bytes。平台之間只允許首圖角色與排序不同，不得進入平台後才重裁、重縮或重做；只有平台明確回傳圖片規格拒絕時，才可對同一完成圖產生相容輸出並更新本規則。
- 已知網址、頁面區段、固定欄位語意與安全選項要作為平台頁面契約重用。頁面版型簽章未變時，直接套用 `preparedPlatformFieldPlan`，不重新掃描整頁；只有分類衍生屬性、依材積決定的物流選項、版型簽章改變或平台實際錯誤需要重新判斷，而且只重查當前區段。
- 四平台一律採用 `batchSections` 整區批次填寫：每區只建立一次欄位索引，文字、數字、原生選單與固定開關同批帶入，圖片同批上傳，整區只驗證一次；分類連動、分類衍生屬性與特殊彈出選單才在該區內依序處理。頁面簽章不變時，禁止每填一欄就重新掃描整頁。
- 按下「帶入這個 Codex 對話」即完成本案四通路例行發布授權。營運中心、工作佇列與自動化程式不得再顯示或等待「確認正式發布四通路／確認上架／確認提交／套用細項」等第二次應用程式內確認；固定欄位與最後送出直接依不可變快照執行。若執行環境本身另有不可略過的安全提示，仍依該環境規則處理，不可假裝繞過。
- 蝦皮固定只使用 EasyStore 官方蝦皮通路同步／編輯頁：先套用進站前已完成的進階商品描述與介紹圖片，再依已準備欄位表完成分類、品牌、屬性、物流、輪播圖片、價格、庫存、細項與發布，不再另外進入蝦皮賣家中心。帳號支援「進階商品描述」時必須開啟並使用 EasyStore 產品描述；頁面只做一次功能存在與套用結果的輕量檢查，不逐張重驗。頁面或平台暫時錯誤時，仍以同一個 EasyStore 通路商品、同一頁與同一份已準備資料續做；不得切換第二條路徑、返回前一平台重新研究、重新修圖或重新整理文案。發布後只到 EasyStore 蝦皮通路商品清單核對一次正式結果。
- 蝦皮官方通路頁操作前先收合內嵌客服聊天。細項代表圖只從目前 EasyStore 商品已存在的繁體完成圖庫選取，依完成圖 URL／素材 ID 對應各細項；不得為細項圖開啟 Windows 原生選檔視窗。EasyStore 共用圖庫或官網同步成功不代表蝦皮細項小圖已帶入；必須在官方同步頁的每一個細項圖片格實際選入其對應完成圖，單一商品、同款多細項、併入既有商品及新增既有商品細項均相同。先完成全部細項圖片，再把包裝重量換算成平台要求的公克數並填入，最後依序按「準備發布」與「上架」，避免延遲視窗與送出按鈕互相攔截。
- 平台內不做來回重複核對：送出前使用預檢資料，送出後只做一次正式清單的最終 SKU／價格／庫存／狀態核對；只有最終結果不一致才從同一草稿的失敗步驟續做。
- 禁止在送出前全面搜尋或瀏覽任一平台商品清單。只有提交結果不明、回應逾時或精確狀態無法判定時，才可用本案件完全相同 SKU 做一次精確查詢；不得用名稱廣搜。
- 助手為工作開啟的頁籤，在該步驟確認完成後必須關閉；每個仍需登入的通路只保留一個登入錨點頁籤，並盡量回到平台首頁或商品列表。不得關閉使用者原本開啟且與任務無關的頁籤。
- 發布失敗或逾時時，先使用本次 job 保存的既有平台商品 ID、草稿 ID、暫存編號與 stage token 復原；只有送出結果仍不明時，才以完全相同 SKU 做一次精確核對。找到原商品或原草稿就從失敗步驟續做，禁止因重試而建立第二筆商品；精確核對找到多筆時停止並標示需要處理。
- 圖片上傳或平台轉檔是非同步步驟時，必須等待處理完成再進入下一步；短暫逾時、頁面重新整理或欄位未保存可自動退避重試，並保留最後可恢復的步驟。
- 使用者原本開啟的 Codex 內建瀏覽器頁籤若仍可讀取但點擊／輸入持續失效，應在同一內建瀏覽器開啟一個工作頁籤，載入完全相同的商品、草稿或同步網址後從當前步驟續做。這是相同網址與相同身分的輸入控制備援，不是第二條上架流程；不可切到主要 Chrome、重建商品或重新準備案件資料。
- 平台顯示「發布成功」視窗不等於真正完成。發布後必須回商品列表或可查驗的正式介面，至少核對商品編號、價格、庫存與狀態；介面與列表結果矛盾時記為待重試，不得回報已完成。
- 自動重試只在身分與目標可確定時執行；一般登入失效先依既定路徑與保存帳密自動恢復，不得直接交回使用者。只有 OTP、CAPTCHA、保存帳密被拒絕、帳號停用或平台持續故障才交由使用者處理。

## MOMO

- 平台商品上限為 1,000；可使用到 1,000 筆，不固定預留 10 個新品空間。
- 庫存數量為 0 只代表售罄，不是自動下架條件。曾經賣過、尤其銷量較高的商品，即使目前庫存為 0，也應維持上架；查不到銷售紀錄時預設保留。
- 只有名額確實不足時，才依 MOMO 銷售紀錄從「零庫存且沒有銷售／銷量很低」的商品開始評估下架。未取得銷售紀錄前不得批次下架；目前名額足夠時不需執行零庫存下架。
- MOMO 明確回傳 1,000 筆名額上限時，每次只釋放一個名額：依序選「零庫存且無銷售」再選「零庫存且低銷量」，未知銷量或曾高銷量一律保留。操作只能暫時下架，不能刪除；正式清單確認上架數量減少一筆後，立即沿用同一 SKU、同一 MOMO 暫存品號與同一 preparedSnapshot 重新發布，不可另建商品或批次下架。
- 有庫存但已下架或違規的商品，須先查明平台狀態與必填資料；資料可查證且名額允許時才重新發布，不得用猜測的 NCC／BSMI 等資料強行上架。
- 新商品配送固定選「甲指（第三方）：000001 台中市圓環東路347號」，此選項對應新竹物流。不要改選其他收貨不便的承運方式。
- 大型商品不可使用超商；應使用上述第三方／新竹物流。小型商品才可依平台實際限制使用超商。
- MOMO 登入通常需要手機 OTP。OTP 只由帳號持有人完成；系統不得繞過二階段驗證。
- MOMO 專推圖只可從已完成台灣繁體化的 MOMO 第 2 張或第 3 張中，挑選角色包含 `cleanMain`、`localizedDetail` 或 `specification`，且 `momoPromotionEligible=true` 的乾淨商品完成輸出；`containsLogo`、`containsText`、`containsContactInfo`、`containsQrCode`、`greenBrandTemplate` 必須全為 `false`。若第 2、3 張都沒有合格圖片，停止 MOMO 發布，不得以宣傳圖補位。
- 專推圖必須在進入 MOMO 前就寫入 `preparedPlatformFieldPlan.momo.preparedFields.firstSubmitMediaGate`，內容至少包含完成圖網址、唯一素材檔名、素材指紋、素材銀行上傳目標及圖文編輯器插入目標。進站後只執行既定上傳與插入；不得等第一次送出被退回「至少上傳 1 張專推圖」後才選圖或製圖。
- 專推圖要先上傳至 MOMO 素材銀行，再從圖文編輯器使用「從素材銀行選擇」插入。插入前先以唯一檔名與素材指紋確認同一素材是否已存在，已存在時直接選用，不得重複插入。直接按「此裝置上傳」後在畫面中看見圖片，不代表資料已保存；儲存後必須重新開啟同一份草稿一次，確認圖文介紹仍包含同一張專推圖，才可按第一次發布。
- MOMO 的商品主圖、廣告用圖與圖文編輯器專推圖是三個獨立欄位；主圖或廣告用圖顯示成功，不能視為專推圖已完成。廣告用圖預設沿用已準備的合規乾淨主圖。第一次按發布前就要依同一 `firstSubmitMediaGate` 完成三處各自需要的圖片，並在重開同一草稿後同時確認專推圖仍存在、甲指第三方出貨地仍顯示 `000001`。三處未齊全時禁止第一次送出；不得把「至少上傳 1 張專推圖」或「出貨／回收地代碼未填」當成正常的探索流程。
- MOMO 草稿重開後可能保留圖片卻遺失部分物流介面狀態，因此第一次發布前的同一輪核對必須同時確認：圖文編輯器仍有素材銀行圖片、廣告用圖、外箱長寬高、重量、常溫、超商／甲指第三方及出貨地 `000001`。只補同一草稿中缺少的欄位，不回頭重做圖片、文案、細項或另建草稿。
- MOMO 發布後正式清單的精確搜尋要輸入賣家 SKU 或 MOMO 品號，並觸發搜尋欄右側的搜尋控制；只按 Enter 或只看到欄位內有文字不算已執行搜尋。每次送出後只做一次精確查詢並核對品名、庫存、售價與狀態。
- 上述 `firstSubmitMediaGate` 同時適用單一商品、同款多細項及加入既有商品成為細項。多細項要先把本組完整 SKU、細項名稱、價格、庫存與各自繁體完成代表圖寫入同一主商品，整筆主商品只共用一張合規專推圖；不得因細項數量增加而改走另一套流程、逐細項重複上傳專推圖，或略過首次送出關卡。
- MOMO 的「此帳號無此功能權限」必須依發生步驟分類，不得直接判定整個帳號無權限：
  - 若在素材銀行選取同名圖片時出現，視為素材路徑衝突；立即改用包含 SKU、細項與內容指紋的唯一檔名，從「此裝置上傳」直接上傳。不得再選同名素材、重新登入或重做圖片。
  - 若唯一檔名直接上傳已成功、只在已上架商品送出新增細項圖時出現，視為目前編輯路徑受限；保留同一 MOMO 品號、完整 SKU 集合與已上傳素材，轉入同一商品的規格異動流程續送。不得另建、複製、重傳或回營運中心重做。
  - 只有規格異動流程也明確拒絕一次後，才將同一商品記錄為 `account-permission-denied` 永久阻擋。
- 平台仍會審核商品；送出後狀態先記為已排隊或審核中，不可直接當成已上架。
- MOMO 曾發生「畫面顯示發佈成功，但草稿中的必填資料沒有保存」：商品列表仍為暫存、庫存變成 0、售價空白，官方商品資料也查不到。每次發佈後必須用提交時的 SKU、售價與庫存逐項比對商品列表，並再以官方商品資料查驗；只有兩處都一致才可記為完成。
- 若出現上述特徵，必須回到完全相同的 MOMO 暫存品號，重新套用屬性、其他資訊、庫存、售價、市價、原廠編號、材積、重量、溫層、超商／甲指第三方、`000001` 出貨地、圖文介紹、特色與半年保固後再發佈。不得另建新品或複製草稿；預期庫存本來就是 0 時，應比對為 0，不可單憑零庫存誤判失敗。

## Coupang／酷澎

- 酷澎依本案 `listingIntent` 執行：新增模式必須沒有既有 Vendor ID；修改模式必須有唯一既有 Vendor ID；加入細項必須有指定父商品 ID。條件不符就停止，不得擅自改成另一模式。只有送出結果不明的復原流程才精確查同 SKU；多筆或不確定時停止。
- 酷澎第 1 張必須是 `cleanMain`，不得有 Logo、文字、地址、電話或 QR Code；`brandedHero` 可放第 2 張，只有平台明確回傳整個圖庫禁用 Logo／文字時才移除。缺少乾淨商品主圖時停止酷澎，不以店面宣傳圖代替。
- 圖片建立頁固定先上傳乾淨首圖，再加入最多 6 張繁體 `cleanMain`／`localizedDetail`／`specification`／`variantRepresentative` 副圖。分類只映射已在平台外判定的樂器葉分類；品牌優先使用已驗證品牌，不能為了避開證明要求任意改成無品牌。
- 「產生商品資訊」是目前建立頁必要步驟，但只准在同一草稿執行一次。只有產生出的細項欄位為唯讀或無效時，才回圖片步驟重新產生一次；第二次仍無效就保留同一草稿並停止，不可重建商品。
- 細項欄位從 `preparedPlatformFieldPlan` 填入：管理者設定的細項名稱優先；單一商品的數量預設為 1，尺寸只有在平台分類強制且案件沒有可驗證尺寸值時才填 `Free`；價格、庫存及賣家 SKU 必須與本次不可變快照一致。
- 配送在進站前依包裝一次決定：宅配固定對應店家配合的新竹物流／新竹貨運；包裝長寬高總和不超過 101 公分且重量不超過 10 公斤時才可同時開啟 7-ELEVEN／全家，超過任一限制就關閉超商。不得改選黑貓、嘉里大榮或其他未配合承運商；平台沒有新竹物流名稱時，只能使用已確認實際交由新竹物流承運的對應自有／第三方宅配選項。備貨天數固定 1 天。
- 商品介紹使用已準備的繁體內容；一般商品責任模式固定 `TW_General`、責任賣家為「尚品樂器行」、未成年可購買及應稅直接套用。產地只能填可驗證資料，不得猜測。
- 按「建立產品」後保存平台回傳的 Vendor Inventory ID，再到正式商品清單以完全相同 SKU 查一次。狀態「審核中」代表已成功送審但尚未正式上架，必須記為 `pending-review`，不可誤報為 `active`。
- `pending-review` 必須保存送審時間、送審後 24 小時的第一次重查時間，以及送審後 48 小時的第二次重查時間。營運中心提供「重查已送出／審核中」按鈕，只讀取曾送出、已有平台編號或仍在審核的商品；重查確認正式核准才改為 `active`，審核未通過才改為 `rejected`／`missing`。平台後台需要登入與人工驗證，因此不得建立沒有登入工作階段卻宣稱已自動核對的排程。
- 售價必須是 10 元倍數。
- 大型商品不可使用超商，依實際尺寸改用宅配／大型物流。
- 平台審核中的商品記為已排隊或審核中，審核完成後再更新為已上架或未通過。

## 官網與蝦皮

- EasyStore 依本案 `listingIntent` 執行：新增模式不得帶入既有 `productId`；修改模式必須有唯一 `productId`／`variantId`；加入細項只能使用指定父商品 `productId`。條件不符就停止，不得建立替代商品。只有 POST 結果不明或逾時復原時才精確查同 SKU；若找到多筆則停止。
- EasyStore API 或後台成功建立商品，只代表資料已保存，不代表已正式上架。商品仍為「未發佈／草稿」時必須在同一商品切換為「已發佈」並完成最後上架；同款多細項還要在公開商品頁核對本案完整 SKU 集合全部存在，才可將 EasyStore 標記為 verified。公開頁缺少任一細項，或出現本案以外 SKU，都不得用相似商品頁替代。
- 蝦皮固定只使用 EasyStore 官方蝦皮通路同步／編輯頁；官網圖文介紹先在平台外完整準備並凍結，帳號支援時在同頁開啟「進階商品描述」並套用「使用 EasyStore 的產品描述」，再將分類、品牌、屬性、物流、圖片、價格、庫存、細項與發布一次完成，不再開啟蝦皮賣家中心或第二條助手路徑。
- 蝦皮同樣依本案 `listingIntent` 執行：新增模式不得對到既有蝦皮商品 ID，修改模式必須鎖定唯一既有 ID，加入細項必須鎖定指定父商品。全部仍在 EasyStore 官方蝦皮通路頁完成，不先開啟商品清單廣搜，也不讓平台頁自行選擇「新增或更新」。
- EasyStore 商品圖片優先使用已準備好的本機完成圖；若同一商品頁的檔案選擇器未能實際帶入圖片，直接改用 `generatedListingImages` 的公開完成圖網址批次加入，維持 `brandedHero` 首圖與既定順序，不得重新製圖或重新研究商品。
- 小型商品符合超商材積與重量限制時，可同時開啟超商與新竹物流；大型商品關閉超商，只使用符合材積級距的新竹物流。
- 黑貓宅急便、嘉里物流及「賣家宅配：大型／超重物品運送」目前都未配合，一律不得自動開啟。尺寸、重量或新竹物流級距無法確認時停止物流自動填寫，不得改選其他承運方式。
