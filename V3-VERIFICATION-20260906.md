# V3 批次續跑驗證

## 第二件 3000118 實測修正

- EasyStore API 的 `description` 與 SEO 摘要回讀為相同內容；僅限制 `metafields_global_description_tag` 不足以避免完整介紹覆蓋 SEO。兩欄現在共用不超過 155 字的摘要，完整圖文仍放 `body_html`，新增回歸斷言。
- 凍結輸出中的描述圖第 4～10 張寬度分別為 542、364、660、600、536、544、521px；「完成圖」標記不能替代實際檔案尺寸檢查。原生上傳前逐張下載及讀取尺寸／檔案大小，未滿 700px 不得宣稱上傳完成；不得默默放大模糊原圖。
- 原品牌模板檔雜湊與實際透明覆蓋層雜湊分開記錄。`templateAssetSha256` 應對應指定模板原檔，透明覆蓋層使用另一證據欄位，禁止把正確套版誤報成模板不符或跳過模板驗證。
- 分類選單、搜尋與送出存在非同步更新；先確認最新畫面及精確 SKU 正式商品，再繼續。點擊逾時不能直接判定平台拒絕，更不能重建商品。
- 3000118 已有 EasyStore 17260545、蝦皮 49067470660、酷澎 1134696126696338；蝦皮描述仍 0/12，圖片驗收未完成，必須續用這些編號。此紀錄不是完成回條。

適用範圍：listing-batch-mtp9mq2j 的交接、快照及登入恢復。不是平台上架完成回條。

- 修正批次一交接便標 processing、卻未建立固定輸入快照的缺口。新增 V3 續跑入口，沿用原案件與快照，完成圖不足時不發布。
- 第一件 3000115 實頁回讀成功；快照 sp6MByVGzp0vATIhhYna-mtpao3v8，11 張來源圖，0 張完成圖，文字仍待研究；publishResult 為 null，尚未送出平台。
- 官網圖片契約核對為 700×1000、7:10、頁首 200 px／20%、Logo 不跨界；本次未變更此設計。
- EasyStore 與蝦皮內建瀏覽器工作階段有效。酷澎保存帳密登入成功，已見賣家首頁。
- MOMO 保存帳密已自動帶入，OTP 空白；需使用者完成動態驗證，不能把此情況誤報為帳密不存在。
- 待修正／未驗證：吉他袋被預判為弦樂器，需於正式欄位計畫前依商品證據修正分類。不得照此建議發布，也不得覆蓋既有不可變輸入記錄。

速度改善不能取代圖片、精確 SKU 與正式結果核對；尚未完成的通路必須保留待處理。

## 續跑實測補充

- 第一件 3000115 已完成 14 個圖片輸出、商品介紹與尺寸依據，寫回同一案件；使用深層值比較驗證 handoff 輸入快照完全未改動（Firestore 回傳鍵順序不同不能當作快照變更）。
- 後端 centralImageReferenceVerification 已回報 verified；官網 700×1000 與蝦皮 1000×1000 品牌圖已顯示，兩者固定頁首 20%、Logo 留在頁首內。
- 已修正完成輸出的分類為樂器配件，不覆蓋原不可變輸入快照。
- 實測預檢攔截 Warranty Duration / Warranty Type 未填；固定半年店家保固已補入同一案件。新增 V3 提醒，在送出前完成這兩個結構化屬性。
- 找到重試指紋漏算蝦皮屬性／分類的缺口：原失敗嘗試會把修正後的資料當作相同內容而忽略。指紋加入屬性、分類及身分狀態；94 項發布與續跑測試通過，純 updatedAt 變更不會觸發重送。
- 登入政策改為各通路獨立：互動驗證只擋住該通路，不阻止圖片準備與其他通路；仍遵守 EasyStore verified 後才接蝦皮。
- 每件顯示完整官網／蝦皮首圖屬進度回報，不是另一次授權關卡。不以中間覆蓋層或半成品代替完成圖。
- 以上不是四平台完成回條；平台狀態仍以同一 job 正式驗證為準。
- 補齊吉他袋／貝斯袋／烏克麗麗袋優先判為配件的規則，95 項測試通過；autoPublishProductListingCase 單一函式部署成功。既有 job 的 preparedSnapshot 不覆寫，平台實際分類修正另留紀錄。
- 3000115 官網正式商品 17260443 已存在。回讀發現舊部署仍將完整介紹寫入 SEO（886／450），在同一商品修正後 API 回讀為 73 字；此次部署含既有 SEO 長度限制。後續商品仍須回讀 SEO，不以本機已有規則推定線上一定生效。
- 3000115 蝦皮 28945742274：EasyStore 匯入後描述仍為 0/12。點可見的「新增圖片 → 從電腦裝置上傳」，先註冊 filechooser，再選本機完成圖，可正常上傳；只點隱藏 input 的嘗試逾時，不能當作平台拒絕或整頁鎖定。
- 以標題前的編輯游標插圖；最後兩張固定店家圖放在兩段說明之後。更新並重開正式商品，已確認 12/12、文字保留。Quill 全文修改採選取全文／清除／剪貼簿貼上並立即回讀，不能將 locator.fill 成功當成內容已正確替換。
- EasyStore 的直式封面匯入蝦皮後會被補白成方圖，不等於已使用獨立設計的蝦皮首圖。改上傳既有 1000×1000 完成图，拖到第一格，再移除被替換的直式輪播圖；更新後正式首圖 URL 已變更，七張輪播及十二張描述仍保留。
- 超商超尺寸不代表新竹不能送：物流各自判定。114×48×19cm／2.6kg 仍符合新竹 S190；新竹225元與大型超重100元開啟，其他物流關閉。新增超尺寸回歸測試。
- 同 SKU 酷澎正式清單 1134696126695392 已為 APPROVED，輪播7張、描述12張，非審核中。MOMO 保存帳密已帶入但仍缺 OTP，僅 MOMO 保留重試，不誤報 missing，不重送其餘已驗證通路。
# 蝦皮圖片實際像素預檢（2026-09-06）

- 適用：新建 preparedSnapshot 且選取蝦皮的案件；既有 job 不重建快照、不重送 verified 通路。
- 先下載每張計畫中的描述圖片，以真實檔案解碼檢查寬至少 700px、高至少 32px、寬高比 0.5～32、JPEG/PNG、不超過 2,000,000 bytes。
- 下載逾時、破圖、尺寸或格式不符時，先在平台外停止，回報精確圖片順序與原因；不得假裝已完成、不得自動把模糊原圖放大。
- 驗證回條寫入新快照 nativeDescriptionImagePreflight；不變動原始來源圖及其不可變輸入快照。
- 98 項測試通過，含小圖、超大串流檔、比例、未選蝦皮不讀取及輸入快照不變測試。
