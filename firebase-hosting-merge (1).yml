rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // 第5階段暫時規則：讓目前 GitHub Pages 前端可以直接讀寫 Firebase。
    // 這是「正式切換測試」用，等功能確認後，需要再改成登入/角色權限版。
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
