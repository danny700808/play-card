const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const s=fs.readFileSync('functions/coursePortal.js','utf8'),start=s.indexOf('async function dailyTeacherCourseReminders('),end=s.indexOf('async function dailyStudentReminders(',start);let queried=false;const ctx={currentTaipeiDay:()=> '2026-09-14',weekday:()=>1,db:{collection:()=>{queried=true;throw Error('Monday must not query')}}};vm.runInNewContext(s.slice(start,end),ctx);(async()=>{await ctx.dailyTeacherCourseReminders(()=>{throw Error('Monday must not send')});assert.equal(queried,false);assert(s.includes("schedule: '0 9 * * 0,2-6'"));console.log('Monday teacher daily notice skips all reads and messages; scheduler excludes Monday');})().catch(e=>{console.error(e);process.exitCode=1});
